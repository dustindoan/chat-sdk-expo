/**
 * Document Handlers
 *
 * Re-exports document handlers from @chat-sdk-expo/artifacts package.
 */

// Re-export everything from the package
export {
  textDocumentHandler,
  codeDocumentHandler,
  detectLanguage,
  documentHandlers,
  documentHandlersByKind,
  getDocumentHandler,
} from '@chat-sdk-expo/artifacts';

/**
 * Theme exports for React Native style objects.
 *
 * Use these constants when you need colors/spacing in:
 * - React Navigation options (headerStyle, drawerStyle, etc.)
 * - Style props that don't accept className (placeholderTextColor, color, etc.)
 * - StyleSheet.create() objects
 *
 * For className-compatible components, prefer Tailwind semantic classes instead.
 */

export {
  colors,
  spacing,
  radius,
  syntaxColors,
  languageColors,
  weatherColors,
  temperatureColors,
} from './colors';
